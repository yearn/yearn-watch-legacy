export enum Network {
    mainnet = 'ethereum',
    fantom = 'fantom',
    arbitrum = 'arbitrum',
}

export const DEFAULT_NETWORK = Network.mainnet;

export enum NetworkId {
    mainnet = 1,
    fantom = 250,
    arbitrum = 42161,
}
